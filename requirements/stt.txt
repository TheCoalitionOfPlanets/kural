# Root venv (venv/) — Python 3.14. Hosts capture, the orchestrator and the
# SraVaani STT worker. Pins frozen from the working environment.
#
# torch/torchvision are NOT listed here: they are cu132 wheels that only exist
# on the PyTorch CUDA index, and are installed separately.
transformers==5.15.0
tokenizers==0.22.2
huggingface_hub==1.28.0
hf-xet==1.6.0
safetensors==0.8.0
sentencepiece==0.2.2
numpy==2.5.2

# The language gate (facebook/mms-lid-126) and the Set B ear (Whisper large-v3)
# add no pins of their own: MMS-LID is a plain transformers Wav2Vec2 classifier
# and Whisper a plain transformers speech-seq2seq, both already covered by the
# transformers pin above. Whisper is loaded on demand rather than at startup —
# see stt.whisper in pipeline/config/realtime.yaml.

# Orchestrator: capture, playback, config, VAD.
sounddevice==0.5.6
soundfile==0.14.0
PyYAML==6.0.3
webrtcvad==2.0.10

# The WebSocket front end (pipeline/server/), which lets the browser in web/ be
# the microphone and the speakers. Not needed for the terminal pipeline —
# `run_realtime.py` imports none of this.
fastapi==0.141.1
uvicorn[standard]==0.52.4
